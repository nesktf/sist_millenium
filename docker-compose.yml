version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: postgres_millenium 
    restart: always
    environment:
      POSTGRES_USER: funny_user
      POSTGRES_PASSWORD: funny_password
      POSTGRES_DB: millenium 
    ports:
      - "5435:5435"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
